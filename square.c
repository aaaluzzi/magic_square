//store 2d array for magic square

/*function isMagicSquare
    check rows/cols
        if sum != 15 return false
    return diagonal sums
*/

/*function main
    int count
    while isMagicSquare(2d array)
        fill 2d array with 0's
        for (int i 1-9)
            do
                int row, col = random indexes
            while array[row][col] != 0
        count++
    print count
    print 2d array
*/